# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'isEazy meteo' do
  use_frameworks!

  # Pods for isEazy meteo
  pod 'Bond'
  pod 'SwiftLint'

end
